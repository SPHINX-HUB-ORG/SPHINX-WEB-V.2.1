import { TEditor, Value } from './TEditor';
/**
 * Add a custom property to the leaf text nodes in the current selection.
 *
 * If the selection is currently collapsed, the marks will be added to the
 * `editor.marks` property instead, and applied when text is inserted next.
 */
export declare const addMark: <V extends Value, M extends import("../..").Simplify<import("../..").UnionToIntersection<import("..").TNodeProps<import("../text/TText").TextOf<TEditor<V>>>>>, K extends keyof M & string>(editor: TEditor<V>, key: {} extends M ? string : K, value: {} extends M ? unknown : M[K]) => void;
//# sourceMappingURL=addMark.d.ts.map