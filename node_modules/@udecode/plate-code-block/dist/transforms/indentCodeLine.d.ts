import { TEditor, TElementEntry, Value } from '@udecode/plate-core';
export interface IndentCodeLineOptions {
    codeBlock: TElementEntry;
    codeLine: TElementEntry;
}
/**
 * Indent if:
 * - the selection is expanded
 * - the selected code line has no whitespace character
 * Indentation = 2 spaces.
 */
export declare const indentCodeLine: <V extends Value>(editor: TEditor<V>, { codeLine }: IndentCodeLineOptions) => void;
//# sourceMappingURL=indentCodeLine.d.ts.map