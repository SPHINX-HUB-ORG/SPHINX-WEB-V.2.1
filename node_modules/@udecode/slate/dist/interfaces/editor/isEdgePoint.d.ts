import { Location, Point } from 'slate';
import { TEditor, Value } from './TEditor';
/**
 * Check if a point is an edge of a location.
 */
export declare const isEdgePoint: <V extends Value>(editor: TEditor<V>, point: Point, at: Location) => boolean;
//# sourceMappingURL=isEdgePoint.d.ts.map